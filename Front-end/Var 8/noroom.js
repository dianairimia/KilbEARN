var clicked[] = [0, 0, 0, 0, 0, 0, 0];
var buttons[] = []
function selectedIcon(name) {
  if (clicked.includes(1))
  {
      var pos = clicked.indexOf(1, 0);
  }
  name.style.border = "2px solid white";
}
